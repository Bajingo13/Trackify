<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Location, for the trip trail the dispatcher watches. FINE is what a
         road position needs; COARSE is declared too so a driver who grants
         only approximate location still gets an app that works. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Camera, for receipt photos and proof of delivery. Declared as not
         required so the app still installs on a handset without one. -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />

    <!-- NOT declared yet, deliberately:

         ACCESS_BACKGROUND_LOCATION, FOREGROUND_SERVICE and
         FOREGROUND_SERVICE_LOCATION are what keep the trail alive once the
         screen locks. They need a background-location plugin to be of any
         use, and Google Play requires a written justification plus a demo
         video for the background permission — an app that asks for it without
         that gets rejected. Add them together with the plugin, not before.

         READ_MEDIA_IMAGES is also absent: the camera plugin captures straight
         to a file through the FileProvider above, so the app never needs to
         read the driver's gallery. -->
</manifest>

/**
 * Settings-facing name for the shared status pill so modules import one
 * vocabulary. Backed by the app-wide StatusPill (same colours everywhere).
 */
export { StatusPill as default } from "../ui";
